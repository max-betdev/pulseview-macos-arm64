# Source checkouts and build outputs are regenerated by the scripts — not tracked.
src/
dist/
logs/

# Packaged artifacts (produced by package.sh).
*.app/
*.dmg

# Out-of-tree build directories, temp icon staging, macOS cruft.
**/build/
.pulseview.iconset/
.pv_logo.png
.dmg_stage/
.DS_Store
